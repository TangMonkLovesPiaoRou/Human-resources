const getters = {
  permission_router: (state) => state.permission.router,
  settings: (state) => state.settings,
  initGlobalState: (state) => state.initGlobalState.globalState
}

export default getters
